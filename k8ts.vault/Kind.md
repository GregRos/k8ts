The [[Kind]] is a hierarchical identifier object that’s used at both runtime and compile time by resources. There are `Kind` subclasses for:

- The api group
- A version with an api group
- A kind with a version and api group

- The api group
- The version
- The kind string

